{% extends 'base.html.twig' %}
{# @var \App\Entity\Vehiculo[] $vehiculos #}
{% block title %}Bienvenido{% endblock %}

{% block body %}
    <h1> {{ 'Bienvenido'|trans }}</h1>
    <a href="{{ path('app_nuevo', {  }) }}" class="new float-right">Nuevo Vehiculo</a>

    <form action="#">
        <input type="search" name="q" placeholder="Buscar">
        <input type="submit" value="Filtrar" class="btn btn-success">
    </form>


    <table class="table">
        <thead>
        <tr>
            <th>Tipo</th>
            <th>Marca</th>
            <th>Modelo</th>
            <th>Color</th>
            <th>Matricula</th>
            <th>Acciones</th>
        </tr>
        </thead>
        <tbody>
        {% for vehiculo in vehiculos %}
            <tr>
                <td>{{ vehiculo['tipo'] }}</td>
                <td>{{ vehiculo['marca'] }}</td>
                <td>{{ vehiculo['modelo'] }}</td>
                <td>{{ vehiculo['color'] }}</td>
                <td>{{ vehiculo['matricula'] }}</td>
                <td>
                    <a href="{{ path('app_ver', { 'id': vehiculo['id'] }) }}" class="show"
                       id="ver_{{ vehiculo['id'] }}">Ver</a>
                    <a href="{{ path('app_edit', { 'id': vehiculo['id'] }) }}" id="edit_{{ vehiculo['id'] }}"
                       class="edit">Editar</a>
                    <a href="{{ path('rematricular', { 'id': vehiculo['id'] }) }}" class="btn btn-secondary">Rematricular</a>
                    <a href="">Vender</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
